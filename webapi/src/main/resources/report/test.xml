<?xml version="1.0" encoding="UTF-8"?>

<mapper>
    <select id='t1'>

    SELECT * FROM dictionary
    WHERE 1=1
        <#if name?? && name != "">
    AND name LIKE :name
        </#if>
        <#if type??>
    AND type = :type
            </#if>

</select>
</mapper>